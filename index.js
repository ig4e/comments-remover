// Legacy extension entry removed — this package is now CLI-first.
// Use `remove-comments` (see `bin/remove-comments.js`) or require the library from `lib/cli.js`.

module.exports = require('./lib/cli');
